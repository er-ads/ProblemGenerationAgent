# Generated by Github Co-pilot 

# Core LLM client (used as `import google.generativeai as genai`)
# Note: the package name may vary; if this entry fails, try `google-generative-ai`
google-generative-ai>=0.2.0

# Numerical & plotting packages used by the evaluator and utilities
numpy>=1.24.0,<2.0
pandas>=2.0.0,<3.0
matplotlib>=3.6.0,<4.0
seaborn>=0.12.0

# Templating / report generation helpers (dataset_evaluator may use templating)
Jinja2>=3.1.0

# Utility / convenience
tqdm>=4.65.0
python-dotenv>=1.0.0  # optional: load env vars from .env during local dev
requests>=2.28.0

# Static analysis / formatting (dev-only; optional)
black>=23.9.1
flake8>=6.0.0

# Notes:
# - These are conservative, compatible ranges intended for a reproducible environment.
# - Pin exact versions or add a lockfile (pip-tools / poetry) for strict reproducibility.
# - If the repository uses a different package name for the Google GenAI client,
#   update the first line accordingly (e.g., google-generativeai).